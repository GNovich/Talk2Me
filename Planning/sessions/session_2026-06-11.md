# Session Report — 2026-06-11

## What was done

### Task 1 — Repository teardown & MLX scaffold (ROADMAP Feature 1) ✅

- Created archival git tag `legacy-tacotron2-waveRNN-stack` before any deletion
- Removed all legacy files: `encoder/`, `synthesizer/`, `vocoder/`, `toolbox/`,
  all `*_train.py` / `*_preprocess.py` scripts, `demo_cli.py`, `demo_toolbox.py`,
  `demo_toolbox_collab.ipynb`, `requirements.txt` (conda spec)
- Preserved `utils/logmmse.py` → relocated to `talk2me/audio/logmmse.py`
- Created `pyproject.toml` targeting Python 3.11+; direct deps: `mlx-whisper`,
  `f5-tts-mlx`, `sounddevice`, `numpy`, `pyyaml`, `onnxruntime`; optional `mlx-lm`;
  dev extras: `pytest`, `ruff`
- New package layout created: `talk2me/{audio,stt,tts,engine}/__init__.py`,
  `talk2me/app.py`, `config/exhibit.yaml`, `questions/`, `saved_audio/`, `tests/`
- Updated `.gitignore`: removed stale saved-model-dir patterns; kept `saved_audio/*`
  with `.gitkeep` exception
- Rewrote `README.md` with concept, hardware target, stack table, setup instructions,
  architecture diagram, privacy note, and pointer to legacy tag
- 5 scaffold smoke tests — all pass

### Task 2 — Audio I/O + VAD (ROADMAP Feature 2) ✅

- `talk2me/audio/io.py`: `Microphone` wrapper (sounddevice, 16 kHz mono float32,
  device by name/index/None-default, gain control, `calibrate_noise()`);
  `Speaker` wrapper (blocking + non-blocking; ~1 s silence pad for tail-cut bug;
  thread-locked `play()`)
- `talk2me/audio/vad.py`: `_SileroGate` (Silero VAD ONNX, auto-downloads ~1.8 MB
  to `~/.cache/talk2me/` on first use) with `_EnergyGate` fallback; stateful reset
  between utterances; `record_utterance(mic, max_seconds, silence_ms) -> np.ndarray`
  API — discards pre-speech, captures until trailing-silence threshold; `build_gate()`
  for pre-warming
- `config/exhibit.yaml` extended with audio knobs
- 7 unit tests, no real hardware needed — all pass

### Task 3 — Local STT with Whisper-MLX (ROADMAP Feature 3) ✅

- `talk2me/stt/whisper.py`: `Transcriber.transcribe(wav) -> TranscriptResult`
  (text, no_speech_prob, avg_logprob, latency_s); model loaded + JIT-warmed on
  first call (warmup dummy silence); empty/None input short-circuits before load
- Default model: `mlx-community/whisper-large-v3-turbo`
- `is_speech()` uses no_speech_prob threshold for false-trigger rejection
- avg_logprob exposed for reference-segment quality ranking (Feature 6)
- 6 unit tests (model-free) + 1 `@pytest.mark.model` integration test
- Total: 18 tests, all pass (excluding 1 model-gated test)

## Verification status

**Functional verification PENDING — no on-hardware audio test this session.**

This session ran as an automated agent without access to the exhibit Mac's
microphone or speakers. The following manual verifications must be done before
marking the pipeline "walking skeleton" ready:

1. Run `python -c "from talk2me.audio import Microphone, Speaker, record_utterance; ..."` on the M-series Mac, confirm sounddevice enumerates the built-in mic and speaker
2. Call `record_utterance()` live — confirm Silero VAD downloads and gates correctly; listen to the captured WAV
3. Feed the captured WAV to `Transcriber().transcribe()` — confirm model downloads from HuggingFace, warms up (first call ~5–10 s), then transcribes (target ≤0.5 s for 10 s audio on M2/M3)
4. Run `pytest -m model` on the exhibit Mac to confirm the integration test passes

**No latency numbers collected** — cannot run MLX inference without Apple Silicon hardware in this session.

## Deferred / known gaps

- Silero VAD ONNX download currently requires network at first use; the exhibit
  must be set up once online, or the model pre-fetched (Feature 14 handles this)
- `talk2me/audio/logmmse.py` is present (preserved from legacy) but not wired
  into the capture pipeline yet — denoise is optional per ROADMAP note
- `talk2me/app.py` is a stub (raises NotImplementedError) — wired in Feature 5

## Next session

PROMPT_QUEUE seeded with Features 4, 5, 6 (F5-TTS-MLX cloning, end-to-end loop,
rolling reference accumulation).
